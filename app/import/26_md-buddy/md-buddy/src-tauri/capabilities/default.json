{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Default capabilities",
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "fs:allow-read-file",
    "fs:allow-read-dir",
    "fs:allow-exists",
    "core:path:default"
  ]
}